# AZL-ARCH-1.0

Author: Aziel Eliab only.

---

AZL-ARCH-1.0                                                                                                          Aziel Eliab · public work identity only




Architecture — azieleliab.com software set
AZL-ARCH-1.0 · runtime 1.6.13 snapshot · 2026-09-06 · Aziel Eliab · not a new slug

  Near 40 because the landing names the tab plus the door plus the kernels plus the local fabric. The tab itself is 34
  (33 live + EmbryoLock stub).

1. Count
  Set                               N            Members

  Software-tab                      34           plain → gate → lock. 33 live + EmbryoLock stub

  Door extras                       2            FragGate kernel, QNM/mesh kernel

  Runtime host                      1            aziel-runtime (on the landing; not a tab engine)

  Local fabric                      1            qnm-node — full node process

  Loopback sibling                  1            anon-broadcast — qnm-node module only

  Named set                         39           azieleliab.com · corpus · godlock.uk list these



2. Planes
FACES (azieleliab.com | corpus | godlock.uk) → DOOR (aziel-runtime) → KERNEL (FragGate + DecisionGATE) → BOOK /
OVERLAY / FABRIC / HARNESS. Beside those: SURVIVAL (azieltether), ASSIGN (miragegrid), SIDE-NET (aznet, azbrowser,
azmail), RECEIPT (temporallock, forgereceipts, peacelock), VAULT (ark, embryolock), CLOCKS (staticclock, chronolock), LOCK
ENGINES, OTHER (glossa, postking, zsolver, azclce, azai).
Every live exec from a face, harness, or MCP still hits FragGate. /p/{slug}/{op} is proxy, not exec. Faces never seat a 25-node cell.
GET /v1/mesh never enables. Clock is not Lock. Vaults never unlock on a Worker.

3. Wiring
 human UI or MCP → DecisionGATE → POST /v1/fraggate/call {slug, op, payload} → live engine or named refuse → AZL-LEDGER / TemporalLock
 record → disposable render. Local fabric acts skip Worker enable and still write a ledger record.


4. Plain + book + overlay + harness
  Slug                      Job in the architecture                                                 Must not

  aziel-corpus              Book. MASTER shelf. Content-addressed.                                  Chatbot memory; public private-file search

  azos                      Ethics overlay. Integrity before exec.                                  Remote shell; exec stub

  azai                      Local OpenAI-compatible runtime.                                        New model; paid-key proxy; Jeeves sovereign

  azbot                     Harness / skill. Local 1.1 profiles.                                    Weights; browser login; silent post

  azbrowser                 Cite-search browse. Pair with aznet.                                    Invented visits; harvest

  azhub                     Blank Key container. Places modules.                                    Interpret modules

  azieltether               Survival of downloads. Prefer-central.                                  VPN; mesh-on public pages

  azinterface               Page cycles including MEMORIAL.                                         Exec catalog engines

  azmail                    Airlock classify. Optional ring.                                        MTA; smtp; login; harvest

  aznet                     Silent verify. Memorial hashes.                                         Host payloads

  azclce                    Inconsistency R/D/P.                                                    Intent detector

  forgereceipts             Local checklist / receipt helper.                                       Court contact; legal advice

  glossafilter              Same intent across peer renders.                                        Authorship stamp

  miragegrid                Ephemeral mesh-id assign.                                               VPN / hop / tunnel

  postking                  Continuity game: remain.                                                A mesh protocol

  staticclock               Five advisory geo fields.                                               Scheduler; a Lock




azieleliab.com software architecture · 39 named · 2026-09-06                                                                                          page 1


AZL-ARCH-1.0                                                                                             Aziel Eliab · public work identity only




  Slug                      Job in the architecture                                   Must not

  ark                       Local vault heuristics. Envelope hash.                    Hosted unlock / store

  zsolver                   Nine ontology nodes. Cap 75%.                             Solve cases; blend missing score



5. Gate, kernels, host, fabric
  Slug                      Job in the architecture                                   Must not

  decisiongate              Pre-exec five gates.                                      Skip for convenience

  fraggate                  Hashed registry. Refuse ledger.                           Become engine #35

  mesh                      Public rollup counts.                                     GET-enable; fake 25 peers

  aziel-runtime             Host for door + catalog.                                  Claim proxy is exec

  qnm-node                  Local process. Cell 25 / 2 bridges.                       Public bind; auto-heal spent ID

  anon-broadcast            Local TTS/desk MP4 + hash.                                Upload proxy; origin hide



6. Locks
  Slug                      Job in the architecture                                   Must not

  chronolock                Advisory 08:30–10:30 local window.                        Be TemporalLock

  codelock                  Perceptual view of source.                                Change meaning

  embryolock                Local scorched vault. Tab stub.                           Hosted Worker; FragGate engine

  employeelock              Hash-chained workbook.                                    Court; UL kit; truth score

  foldlock                  Tether-word suppression on UTF-8.                         Zip; file crypto

  godlock                   ABAD score + public stress node.                          VPN / anonymity net

  mialock                   Event map + Doe leads + heat.                             ID claim; live tracking

  peacelock                 Chosen silence as a receipt.                              Invent a transcript

  shadowlock                Zero-retention observe of your list.                      OS hook

  spectrallock              256px overlay preview.                                    Lab spectrometer

  temporallock              Receipt primitive genesis/append/verify.                  Chain model sentences

  trajectorylock            Geometric test prototype.                                 Certified forensic instrument

  veillock                  Local camera/screen on this device.                       Call intercept

  vibelock                  Speech physical-consistency risk.                         Liveness proof

  whistlelock               Local drop + dead-man copy.                               Mailer; send/release



7. Close tests
  • 34 tab + 2 extras + runtime + qnm-node + anon-broadcast = 39 named.
  • No Lock must call another Lock to boot.
  • Every live exec names FragGate or local loopback.
  • Clock ≠ Lock. EmbryoLock stays local-not-hosted. MirageGrid hop stays stub.
  • Public GET /v1/mesh does not enable.
azieleliab.com is the literary landing + software strip + /runtime proxy. Identity Aziel Eliab only. Snapshot 2026-09-06.

  If the files hold, the name was never the point.




azieleliab.com software architecture · 39 named · 2026-09-06                                                                             page 2
