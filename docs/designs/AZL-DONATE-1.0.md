# AZL-DONATE-1.0

Author / identity: **Aziel Eliab** only.

**AZL-DONATE-1.0** · runtime 1.7.7 cite · 2026-09-10 · Aziel Eliab

Cite-only. **Not a Softwares-tab product. Not a FragGate slug.** This ingest does **not** enable remain-off items. Do not invent wallet addresses or tokens.

Canonical rails live on hubs. Runtime only links.

---

## 1. What this paper is

Follow-through plan for donation on **aziel-runtime** and the **download-tracker** footer pattern.

Donation is not software. It is not a catalog slug. It is not an engine. It does not get `LIVE_OPS`. Hubs already host the public-plane rails. This Worker cites the plan and points at the canonical URL.

Companion law: [RL-WP-0.1-runtime](RL-WP-0.1-runtime.md) §7 (Donate strip; static; no KV) and [RL-WP-0.1-library](RL-WP-0.1-library.md) §8 (library island). Constitutional OFF: [REMAIN-OFF-BY-DESIGN-2026-09-10](REMAIN-OFF-BY-DESIGN-2026-09-10.md). Do not enable remain-off items from this cite.

## 2. Canonical URL

```
https://www.azieleliab.com/donate
```

That is the only public donate target this runtime may name.

Hubs may also show a Donate strip (library, godlock.uk) that **links the same URL**. Library already has a static tab. Addresses, copy buttons, optional receipt notes, and network QRs — if shown — live on the hub page after operator paste. This paper does not invent them.

## 3. Runtime Worker (this repo)

One footer line only:

**Donate** → `https://www.azieleliab.com/donate`

Rules:

- Do **not** duplicate five QRs on runtime.
- Do **not** paste wallet addresses into this Worker.
- Do **not** route donations through Workers KV, D1, or Durable Objects.
- `donation.addresses` stays `null` until the operator pastes on the hub.
- `donation.kv` stays `false`. `donation.link_only` is `true`. `donation.qrs` is `false`.
- No legal name. No home. No “support my custody case” copy.
- No Node Gate. No invented `CLOUDFLARE_API_TOKEN` or other tokens.

The existing static donation JSON (networks the operator already controls: Bitcoin, Lightning, Ethereum, Solana) remains a cite of RL law. It is not a hosted wallet list.

## 4. Product download-trackers (pattern)

This repo does **not** own product Workers. Copy this footer into those repos.

One footer line:

**Support the work** → `https://www.azieleliab.com/donate`

Same rails rule: link only. Do not copy hub QRs, addresses, or receipt forms onto `*-download-tracker` Workers.

## 5. What stays off

- Softwares-tab product named Donate / AZL-DONATE
- FragGate slug `donate`
- Invented wallets, seeds, or payment tokens
- KV / D1 / Durable Object donation counters
- Five QR images on runtime or download-trackers
- Mesh, QNS proxy, Node Gate, or any remain-off item “so donors can see status”

Fix the walk first. Rate limit second. Donation third. The tab funds the cycle cap so the public surface can stay up. It does not lower the KV bill by itself.

## 6. Cite

Eliab, Aziel. (2026). AZL-DONATE-1.0 [Design]. https://github.com/AzielEliab/aziel-runtime/blob/main/docs/designs/AZL-DONATE-1.0.md

Identity **Aziel Eliab** only.
