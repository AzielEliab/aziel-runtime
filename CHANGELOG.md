# Changelog

Public identity: **Aziel Eliab** only.

This file exists so Glama / crawlers that look for a repo-root changelog see the **current** MCP server version first.

## 2.0.0-rc1 (current)

- VeilLock `inject` / `intercept` / `facetime` refuse `FG-LOCAL-ONLY` (registry status `local_only` wins over the named stub verb). EmbryoLock wipe / scorch / unlock stay `FG-STUB`. `dry_run` still writes no ledger. `/llms.txt`, `/cite.json` `suite_tip`, and `GET /v1/software` publish `version_id` from `CF_VERSION_METADATA.id` at serve time, or from deploy var `VERSION_ID` when the binding is unset. Unbound isolates keep `version_id: null`. No UUID is stored in the repo. Softwares stay 42. Ask Jeeves stays suite help, not a Softwares card. Identity Aziel Eliab only.
- MCP first call is the tools/list name `Softwares`. `initialize` told agents to call Softwares while `tools/list` only published `runtime_software`, so that call returned `FG-HALLUC-TOOL` and Glama Activity stayed at 0 tool calls. `Softwares` replaces `runtime_software` on the 36-name list. `runtime_software` and `softwares` stay tools/call aliases and are not extra listed names. A successful `tools/call` posts Glama usage telemetry (`server` `AzielEliab/aziel-runtime`) without blocking the MCP response. The stdio bridge posts the same body when the Worker did not already mark the response. Softwares stay 42. Ask Jeeves stays suite help. FragGate stays the single door. Identity Aziel Eliab only.
- Softwares honesty pass. Catalog cards name `public_door`, `public_door_ops`, `stub_ops`, `door_label`, and `open_live_door`. VeilLock stays local only: the human list says Open local desk, describe says product door none and FG-LOCAL-ONLY, and the product page does not offer a public curl door. EmbryoLock wipe, scorch, and unlock stay on the card as disabled FG-STUB controls. AZChat stays LIVE+bound with the product mesh hop off. Whitestone stays a live Worker with FragGate status none. Trades-Runtime stays a cite-only extra (`live_backends` false), not a Softwares card. Softwares stay 42. Ask Jeeves stays suite help, `software_tab` false. `tools/list` stays 36. FragGate stays the single door. Identity Aziel Eliab only.
- TemporalLock and ForgeReceipts join the automatic wall with FragGate and ChainLock. A ledger op that stamps the acts chain also mints a TemporalLock genesis and a ForgeReceipts receipt, and reports each only when the hash recomputes. `fraggate_call` reports the pipe copies when those hashes exist and does not mint a second pair. Reads, dry_run, and admission refuses do not mint them. A background job stays Running until a receipt hash exists. `tools/list` stays 36. No new MCP tool.
- ChainLock is automatic on the same wall as FragGate. Start here is call the Softwares tool. A ledger op stamps the acts chain after a successful body: `decisiongate_check`, `library_lookup`, `memory_observe`, `memory_resolve`, `memory_calibrate`, `mesh_join`, `mesh_enable`, `mesh_heartbeat`, `mesh_leave`, `mesh_broadcast`, `runtime_run`, `runtime_session_exec`. `fraggate_call` reports the session-chain pipe stamp only when `entry.h` is a real hash. Lamb Lens, SweepGate, Sentinel, TemporalLock, RoseClock, and ForgeReceipts run inside that pipe. Reads, dry_run, admission refuses, and `chainlock_*` do not take an extra stamp. A background job stays Running with ChainLock not done until a receipt hash exists. `tools/list` stays 36. No new MCP tool.
- FragGate is automatic. A Softwares or runtime tool enters the door first, then runs. Callers do not start with `fraggate_list` / `fraggate_describe` / `fraggate_call`. Those four stay for diagnostics. `tools/list` stays 36. No new MCP tool. confirm and dry_run honesty is unchanged. Background still returns Running until a receipt hash exists.
- Easier door calls and honest background. Start here is one door: `confirm=true` writes, `dry_run=true` previews and writes nothing, `background=true` on `fraggate_call` returns Running until a receipt hash exists. Done is not claimed without that hash. A missing job is Quiet, not Done. CLI `aziel-runtime call <slug> <op>` goes through FragGate. `aziel-runtime service status` reports Running or Quiet and does not call the door. `tools/list` stays 36. No new MCP tool. Identity Aziel Eliab only.
- First-hour path on the human workspace. A Start here strip names Ask Jeeves, AZAI Guide, and the domain tabs. Show me around scrolls and highlights Jeeves, the AI tab, Softwares, Interface, mesh, FragGate, and receipts. It does not cover those desks or remove them. Empty states and one-line "what is this desk" hints point at the coaches. Epistemology, adaptive counts, emoji voice, and the laugh-then-answer path are unchanged. Softwares stay 42. Ask Jeeves stays suite help, `software_tab` false. Corpus stays the Elroi sub-tab, not a top-bar tab. `tools/list` stays 36. Identity Aziel Eliab only.
- Ask Jeeves uses a light emoji voice. The laugh is `Ha! Splendid. 😄`. A suite-help reply places one mark on the useful paragraph (`✨` `🗂️` `📚` `📖` `🧾` `👋` or `🙂`). The Why line stays plain. A refusal stays plain. Epistemology, triad scoring, and adaptive counts are unchanged. Softwares stay 42. Ask Jeeves stays suite help, `software_tab` false. `tools/list` stays 36. Identity Aziel Eliab only.
- Ask Jeeves laughs when a corpus easter egg matches, then still answers. The laugh is `Ha! Splendid.` on `laugh` and at the start of `answer`. The published egg id, caption, and bitmap stay on the reply. Lamb Lens, the shelf, the Library tab, other sources, and the triad still run after that beat. Adaptive counts are unchanged. Softwares stay 42. Ask Jeeves stays suite help, `software_tab` false. `tools/list` stays 36. Identity Aziel Eliab only.
- Ask Jeeves and AZAI Guide keep a cross-user adaptive count (`JEEVES-ADAPT-1.0`). A confirmed ask stores topic, assertion, corpus-status, and allowlisted hash totals. It does not store the question, mail, secrets, or a session id. `dry_run` stores nothing. A learned sentence is still a candidate: Lamb Lens, then the same triad, `free_pass` false, `believed` false. It does not change the epistemology assertion. The shared document can carry `last_receipt_hash` after the local ACT-RECEIPT. Softwares count stays 42. Ask Jeeves stays suite help, `software_tab` false. `tools/list` stays 36. Identity Aziel Eliab only.
- SOT-SYNC-1.0 outlets are probed together. One unreachable site keeps last-known inventory and does not stall the other outlets or the desk. Confirm is operator consent, not tenant auth, on HTTP, FragGate, and `interface/orchestrate`. Those three doors share one ACT receipt. `/cite.json` `suite_tip` and `/llms.txt` `suite_version` / `git_sha` / `softwares_count` are the live `GET /v1/software` projection. `version_id` stays null until a live body exposes one. Identity Aziel Eliab only.
- SOT-SYNC-1.0 pull plane. `GET /v1/mesh/sot` and `GET /v1/mesh/outlets` read the suite tip from `GET /v1/software` (version, git sha, Softwares count, card versions). `version_id` stays null. `POST /v1/mesh/sot-sync` is dry_run, then confirm. Confirm mints ACT-RECEIPT-1.0 and updates `last_applied` only where a write succeeded. Unreachable outlets keep last-known inventory. `mesh_broadcast` stays hash-only. Nodes and Live Nodes are unchanged. Ask Jeeves is not a Softwares card. Operator calls are FragGate `mesh` ops `outlets` / `sot-status` / `sot-sync` and interface calls `mesh_outlets` / `mesh_sot_status` / `mesh_sot_sync`. `tools/list` stays 36. Aziel Elroi Eliab stays `#elroi-pane`, not a top-bar domain. Corpus stays a sub-tab on that pane. Paper: [`docs/designs/SOT-SYNC-1.0.md`](docs/designs/SOT-SYNC-1.0.md). Identity Aziel Eliab only.
- Audit of the 2026-09-25 drafts (`docs/AUDIT-2026-09-25.md`), including VibeLock #14. Interface seal re-hashes the public ACT copy from the corpus tip, records outcome failed unless FragGate returns FG-OK and the engine result is ok, and refuses passphrase-shaped fields. `POST /v1/interface` is billed as `fraggate_call`. A live seal needs a prior worker plan for that slug and op. A dark corpus tip skips the public append. VibeLock analyze refuses raw container bytes. The card states heuristic physics and related signals, experimental linguistics, vibration only with a body-coupled track, ffmpeg for compressed local files, and no accuracy percentage. AZAI sets `pins_read`, `mesh_read`, and `corpus_searched` only after a real read, and the ACT `output` stays a count sentence. Elroi stays `#elroi-pane`: Published work, Corpus, and Ask Jeeves are sub-tabs. This audit does not add a top-bar Corpus domain or a second author tab. Identity Aziel Eliab only.
- Ask Jeeves and AZAI Guide score every candidate with the suite triad (`GUIDE-REASON-1.2`). Lamb Lens still runs first. The public shelf and the Library tab (aziel-corpus, whitestone) are next. Other sources follow. Corpus hits, Library-tab cards, and outside or suite-help text each receive the same `aziel.triad.v0.3` assembly (CLCE on that candidate; SPRE and PhysLing unverified, so the final stays null). A shelf hit is not a free pass: a CLCE triple under 0.3 stays `uncertain` whether the text came from the shelf or from anywhere else. Two hits that do not share words are `conflict`. `believed` stays false. Softwares count stays 42. Ask Jeeves stays suite help, `software_tab` false. `tools/list` stays 36. Identity Aziel Eliab only.
- Ask Jeeves and AZAI Guide reason corpus-first (`GUIDE-REASON-1.0`). A question is clarified, the public shelf is pulled (sample MASTER, or `CORPUS_D1` records when bound), then this build's 42 Softwares and the human desks supply the next click. An empty shelf says so. An unreachable shelf keeps the last-known sample and does not invent a row. `jeeves_help` and `learner_guide` are the calls. `learner_guide` does not write memory. `dry_run` stores nothing. Learn and seal still wait on confirm for writes. Ask Jeeves stays suite help, `software_tab` false. Softwares count stays 42. `tools/list` stays 36. Identity Aziel Eliab only.
- Ask Jeeves is suite help on the human interface (`POST /v1/interface` call `jeeves_help`, FragGate `aziel-corpus` / `jeeves`). It is not a new Softwares card. Softwares count stays 42. Isolation `software_count` stays 33. Named-tool JSON-LD `isPartOf` is Aziel Corpus, `software_tab` false. Easter eggs are the corpus trigger map from `AzielEliab/aziel-corpus` `workers/download-tracker/src/jeeves.js`. Bitmaps are cited from `workers/download-tracker/public/jeeves-*` (33 files, plus 7 `branding/` copies) and are not hosted here. `dry_run` stores nothing. `tools/list` stays 36. Identity Aziel Eliab only.
- AZChat Softwares card (Social) states the product contract: join from the all-rooms list, a hosted room appears in that list, and a private room requires a passphrase. No new room-list or passphrase backend was added in this runtime. Identity Aziel Eliab only.
- VibeLock Softwares copy states the design: AI deepfake risk in mp4, mp3, and other audio and video, using physics, linguistics, vibrations, and related signals. Hosted analyze and detect still score posted features or limited PCM and cite those channels. Container decode stays on the local package. No accuracy number is published. AZAI learn notes cite that contract, and cite channel scores only when analyze ran or the operator supplied known channels. AZBot plans for VibeLock stay local until seal. Identity Aziel Eliab only.
- AI tab desks. AZBot is a worker peer: intake, plan, status, and handoff. A plan does not call FragGate, merge, or deploy. Seal still requires confirm and is the only path that can dispatch a live step or append the public receipt chain. AZAI is a learner: notes cite a domain, design paper, software slug, local receipt hash, or operator-supplied pin id. The learner does not query 4DMap and does not read the live mesh roster. Catalog Learn notes do not search the shelf. Guide does, corpus first. AKM observe and adaptive recall wait for confirm. Belief is not truth. `tools/list` stays 36. Identity Aziel Eliab only.
- Workspace top bar is domain tabs (`src/ui-domains.js`). Each tab opens that domain's catalog cards. AZnet includes AZBrowser. Forensics includes 4DMap and SpectralLock. Games includes Post-King Chess. Social includes AZChat and AZMail. Every catalog Software is in one tab. MASTER-33 isolation domains are unchanged. The interface honesty strip is removed. ACT-RECEIPT-1.0 fields unchanged. Identity Aziel Eliab only.
- Interface desk uses the existing workspace design (Softwares `dash-card` tiles, operator rack, receipt board, honesty strip). VeilLock is a local-only tile on that grid, not a separate skin. MCP, node-mesh awareness, and the forensic seal sit in the same tiles. ACT-RECEIPT-1.0 fields unchanged. Identity Aziel Eliab only.
- Human-side interface orchestration (IFACE-ORCH-1.0). `POST /v1/interface` and MCP method `interface/orchestrate` share one planner with the workspace pane `#interface-panel`. VeilLock stays `local_only` with `public_door_ops` empty (`veillock-runtime-ui-1`). Safe calls `describe` / `join_plan` / `engulf_plan` / `status_report` / `runtime_ui` do not launch, join, register a camera, return a key, lift a veil, or append the public receipt chain. `mesh_awareness` does not read or change Live Nodes. `seal` requires `confirm: true`, mints ACT-RECEIPT-1.0 `hash` / `request` / `output` / `event` (attempt ids unchanged), and appends the public chain only through the existing fail-open path. A sealed live op dispatches through FragGate and the receipt names the code that returned. `tools/list` stays 36 names. Download counters stay on their existing routes. Identity Aziel Eliab only.
- Design cite: `docs/designs/4DM-WP-1.0.md` names 4DMap **0.3.0**, the same version as the Softwares card (`GET /v1/software` version and one_line already match live). Historical 1.7.6 notes that synced product 0.2.0 stay. AZInterface card stays package **0.1.0** suite shell. `tools/list` stays 36. Not a Worker deploy. Identity Aziel Eliab only.
- Audit harden: MCP `dry_run=true` still writes nothing and stamps no ledger tip, and it still runs FragGate allowlist checks. Hallucination, stub, unknown-op, and local-only calls return the same `FG-HALLUC-TOOL` / `FG-STUB` / `FG-UNKNOWN-OP` / `FG-LOCAL-ONLY` code and message a `confirm=true` call would get. `MCP-DRY-RUN` is returned only when that call would have been allowed to proceed. Mutate responses (MCP confirm, dry_run, confirm-required, HTTP FragGate call, session mutate, and workspace isolation) state that confirm is consent, not tenant auth, and that it does not upgrade shared public-demo isolation. No tenant auth invented. `tools/list` stays 36. FragGate stays the single door. Chief deploys the Worker after merge. Identity Aziel Eliab only.
- Catalog sync with merged Softwares packages. 4DMap card version is **0.3.0** (package main, PR #12). Public FragGate ops add `memory_cite`, `memory_observe`, `library_pin`, `plot`, `possibility`, `pattern_recall`, `lattice_tip`, and `poison_refuse`, plus package aliases `ingest_pin`, `plot_pins`, `score_hooks`, `possibility_cite`, and `lattice_tips`. Those ops run on an in-process 4DM-CARD lattice (paper date × event × place, labeled possibility, hashchain tips, feature-hash refuse). SweepGate still isolates explicit poison tokens (`poison` beside `payload` / `token` / `inject`, and the existing mark list). A 4DMap refuse receipt that names `poison_refuse` and `payload_stored` apart is not that token, so the door can return the receipt. Earlier inspection verbs stay on the multi-axis card store. `truth_score` / `lumen_panel` / `invent_mark` / `backdate_class` stay stub. 4DMap stays a Research inspection frame. FragGate stays the single door. AZInterface package (PR #18) is still **0.1.0**. The Softwares card one_line and description name the suite shell and keep the five page cycles. Local package ops `pipeline_arch`, `withdraw`, `scorch_local`, and `pair_*` stay off the public door (`FG-UNKNOWN-OP`). Placement stays human-ui. `tools/list` stays 36. Chief deploys the Worker after merge. Identity Aziel Eliab only.
- Glama discovery copy (claim file + docs, not a Worker deploy and not a new MCP tool). `glama.json` description leads with the plain card sentence: AI assistants run 40+ research tools through one door, install on Glama, list / describe / call, every call can leave a receipt. Count is 40 live Softwares catalog products behind FragGate (mesh and memory are not Softwares-tab products; VeilLock stays local_only). `tools/list` stays 36. How to use is two steps: Install / Add to Glama, then `fraggate_list` → `fraggate_describe {name}` → `fraggate_call {name, op, payload, confirm:true}`. Worker remote stays `https://aziel-runtime.vibelock.workers.dev/mcp`. Example first call stays `decisiongate_check` with `dry_run:true`, or `forgereceipts` receipt. Install order stays Install Server → remote Worker → local stdio last. `glama.json` `version` stays Worker `2.0.0-rc1`; description still names Glama Install Server release 2.0.7. Identity Aziel Eliab only.
- Human CLI: `aziel-runtime` with no arguments prints a short welcome and the next command. `--help` lists session commands; version history stays in this file. Session commands print a short summary, and `--json` prints the same object as before. The suite pack `aziel-runtime-suite.json` includes `readme` and `quickstart`. Stdio MCP stdout stays JSON-RPC. No MCP tool added or removed. Identity Aziel Eliab only.
- FED-MESH-BLOCKLIST-1 folds digit lookalikes the same way as aznet (`cursor/blocklist-fold-1dc6`): after separators are removed, `0 1 3 4 5 7` are read as `o i e a s t` for substring matching and for exact and hyphen-part matching. `child.porn`, `child-porn`, and `ch1ldp0rn` are refused. `sussex` and `analysis` are allowed. The shared cases are pinned as `blocklist_fold` in `fixtures/fed-mesh-vectors.json`. No new MCP tool. Identity Aziel Eliab only.
- FED-MESH-BLOCKLIST-1 is the union of the relay list and AZN-BLOCK-1.0 (aznet branch `cursor/azn-name-ledger-1dc6`; `main` has no blocklist file). `csam` is refused. `POST /v1/mesh/relay/isolation` is the isolation route (path, body, and response in the spec). `/v1/fedmesh/<tail>` aliases `/v1/mesh/relay/<tail>`. GET still never enables. No new MCP tool. Identity Aziel Eliab only.
- FED-MESH-1.0 per-handle slots and ethics isolation. Each handle has 4 reserved hub-mirror slots (`ae`, `corpus`, `godlock`, `hdj`) and 3 user `.aziel` names, plus the automatic `<handle>.aziel` name. Restore accepts only an object this relay already hash-verified. A versioned name blocklist isolates the claiming handle. A self-signed isolation stores a reason code and an evidence hash, never the bytes. An appeal requests a re-check and does not clear isolation. Content classifiers run on the hosting node and are absent here. MirageGrid Cap-7 factory `.az` names are unchanged. No new MCP tool. Identity Aziel Eliab only.
- FED-MESH-1.0 mesh security. Friendly `.aziel` claims carry an 8-bit proof-of-work and stay pending until 72 hours and 2 witness handles. The first valid final claim wins. Equivocation proofs flag one handle. Ref and name rollback is refused. Hash mismatches stay unstored (`FG-GATE-REFUSE`). The Worker does not execute peer code, does not rank handles, and does not cut a peer off the mesh. Scanner is absent. Island and quarantine receipts do not change suite radios. No new MCP tool. Identity Aziel Eliab only.
- FED-MESH-1.0 name records. The relay accepts, anchors, and serves signed `.aziel` names the same way it serves ref updates. `<handle>.aziel` is self-certifying. A friendly name goes to the first valid final claim, with 3 user names per handle. Transfer and release are signed by the current owner. Forks are refused. `.az` stays normal DNS except the Cap-7 allowlist and the AZ.* hub names. No new MCP tool. Identity Aziel Eliab only.
- FED-MESH-1.0: Local-First Edge Mesh. Raw data, signing keys, and heavy compute stay on the local node. By default the mesh carries signed receipts, state digests, and ref updates. Raw data moves only on an explicit end-to-end encrypted share. The Worker is one relay among many and never requires plaintext. It stores and serves signed ref updates and anchors them with ChainLock and TemporalLock. Full objects stay on peers. A public object cache is capped (4096 bytes, 64 objects, 64KiB) and refuses a hash mismatch. Late rollups and ref updates with a valid chain are accepted. Forks are refused. `verified_handles` counts distinct live handles and is not folded into the published Nodes / Live Nodes pills. No new MCP tool. Paper: [`docs/designs/FED-MESH-1.0.md`](docs/designs/FED-MESH-1.0.md). Identity Aziel Eliab only.
- Factory SoT alignment (MirageGrid cap7.js): real hub duplications are `azgrid`, `azcloak`, `azvault`, `azshift`. False sites are `azbooth`, `azflag`, `azstandby`. Shift clock is StaticLock; catalog product remains StaticClock (slug `staticclock`), not a second Softwares product. HDJ hub origin stays `https://www.hedidntjump.com/` because `seo.js` locks that origin. Standard internet still reaches AZ domains only. Cap-7 stays not internet-reachable. Factory honesty stays LIVE.
- OPERATOR OVERRIDE 2026-09-24 — Cap-7 / AZ domains. Standard internet does not reach Cap-7. Cap-7 is the MirageGrid auto-generate `.az` duplication layer (four real hub duplications: `azgrid` / `azvault` / `azbooth` / `azflag`; three false sites: `azcloak` / `azshift` / `azstandby`) shifting with StaticClock and MirageGrid cloak, paired with AZVPN. Factory duplication, shuffle land, and hosted cite are LIVE (`mesh_name_icann: live`; no SLOT hedge on that factory path). Internet reaches the AZ domains through the four hub HTTPS links (`AZ.AzielEliab.AZ` ↔ azieleliab.com, `AZ.AzielCorpusLibrary.AZ` ↔ azielcorpuslibrary.net, `AZ.Godlock.AZ` ↔ godlock.uk, `AZ.HeDidntJump.AZ` ↔ hedidntjump.com): `public_icann: true`, `resolves_to_hub: true`, shuffle once to 1 of 4, stand alone, immutable after hubs go down, anchored by live nodes. Cap-7 itself stays `resolves_to_hub: false` / `public_icann: false` / `internet_reachable: false`. `icann_tld_az: false` (not an ICANN ccTLD purchase). `radio_phy: false`. FragGate remains the single door. No new MCP tool.
- Official MCP Registry [`server.json`](server.json) at the repo root (remotes only; no `packages` entry because `package.json` is private). Registry `version` is Worker `2.0.0-rc1`. Glama release 2.0.7 stays a Glama label in README / `docs/GLAMA.md` and is not the registry version. `mcp-publisher publish` is out of band. `tools/list` stays 36. Identity Aziel Eliab only.
- Glama install conversion (docs + machine surfaces). Install order is one-click Install Server on Glama, then remote `POST /mcp`, then local stdio. First call stays `fraggate_list` → `fraggate_describe` → `fraggate_call` (foldlock/fold-preview, or `decisiongate_check` with `dry_run=true`). `tools/list` count is 36. `glama.json` `version` stays Worker `2.0.0-rc1`; description names Glama Install Server release 2.0.7 and adds keywords softwares, decisiongate, receipts. `/cite.json` `glama`, `/llms.txt`, `/.well-known/mcp/server-card.json`, and `/help/glama.txt` point Install / Try on Glama. No new MCP tool. No hub HTML chrome. Identity Aziel Eliab only.
- Receipt attempt surfaces (follow-on, engine unchanged): `/openapi.json` schemas `ForgeReceipt`, `SessionReceipt`, and `ResultEnvelope` list `request_id`, `attempt_n`, `parent_receipt_id`, `correlation_id`, and `outcome`. `fraggate_describe` for ForgeReceipts uses the Softwares card description (those names hashed into the receipt; `ledger_tip.prev` is call-order only; `prev_is_retry_parent` false). `GET /v1/skill` adds the same linkage and does not treat ledger `prev` as the retry parent. Not a forensic finding. Sealed history is not rewritten. Identity Aziel Eliab only.
- Receipt attempt link (additive on new receipts): ForgeReceipts canonical hash now covers `request_id`, `attempt_n`, `parent_receipt_id`, `correlation_id`, and `outcome`. Same note with a different `attempt_n` is a different hash. `parent_receipt_id` is the prior attempt's receipt hash (null on the first). FragGate ledger `prev` stays call-order only and is not the retry parent. The same fields are on the FragGate ResultEnvelope and on new session receipts. ACT event metadata may carry them (still not a Softwares slug). Receipts sealed without `attempt_n` keep the older hash. Not a court filing and not a forensic finding. Identity Aziel Eliab only.
- SpectralLock vendor sync after spectrallock#16 (`92fcc3b031b3c6e1bf398d4263092e20d9bad3c5`, product 0.3.1): copy `overlay.js` byte-for-byte. Wheel paint (Spectral Harmonic Wheel) is a membership-tint plane separate from the spectral triad (Tazel `#1EC9A5`, Vyrn `#C00066`, Zero `#6F6485`). Restore lost pigment is LIVE on FragGate ops `pigment` and `restore-pigment` (`listPigment` / `pigmentFromB64`). That path estimates faded signal still in the pixels and refuses `SL-PIGMENT-GONE` when the evidence is gone. `pigment_recovery` is true only on that path. Overlay, inject, and unredact receipts keep `pigment_recovery` false. AMOE stays on the suite project map and is not a live product. Unredact / recover / handwriting stay off the FragGate door (product Worker `/v1/unredact`, `/v1/recover`, `/v1/handwriting`). Rehashed `ENGINE_DIGESTS.spectrallock` `16281860ab0787f2485ea2530fbbcecf968009da3ae1c4f2b6e689a15a9ad586`. Catalog version `0.3.1`. Softwares `one_line` / describe / skill cite the wheel plane, live pigment restore, and AMOE. Rehashed `ENGINE_DIGESTS.azbot` `7e0843602e8bfed071ab1a20aad32d6f22f8330be3526e29c29ffcfb16892b89` (banner/keys only). Stubs stay spectrometer, forensic, invent_mark. Identity Aziel Eliab only. Lamb Lens: Service → Clarity → Peace.
- OPERATOR LOCK 2026-09-21 seal — Live Nodes chrome stays on one aggregate. `GET /v1/mesh` reads `site_viewers` once (`site_live_viewers_read: "single-key"`). `live_nodes` remains `human_mesh_users + site_live_viewers`. `live_nodes_generation` bumps only when a stored hub viewer count changes. `live_nodes_tip` is `generation:human_mesh_users:host=count` for this JSON. Roster fan-out and join no longer rewrite that key (a stale full-state save was dropping in-flight hub heartbeats). Per-host keys merge by newest `last_seen`. This isolate will not walk a seal backwards. Hubs paint `live_nodes` / `rollup.mesh` only — do not add a local `/count`. GET still never pulls hub `/count`. Missing or expired rows stay 0. No MCP tool added or removed. See [`docs/NODE_MESH.md`](docs/NODE_MESH.md).
- OPERATOR LOCK 2026-09-21 chrome — `rollup.live` is no longer published. It was roster `presence=live` on every plane and, after `{slug}-worker` fan-out, matched `software_nodes`. Public Live Nodes stays `live_nodes` / `rollup.mesh`. Never paint `software_nodes` or `rollup.live` as Live Nodes. Roster presence=live is `rollup.all.live`. Softwares presence=live is `rollup.software.live`. `rollup.public_live_nodes` is `"mesh"`. Do not invent users. See [`docs/NODE_MESH.md`](docs/NODE_MESH.md).
- OPERATOR LOCK 2026-09-21 addendum — public **Live Nodes** include **current website viewers**. `GET /v1/mesh` `live_nodes` / `rollup.mesh` = `human_mesh_users` + `site_live_viewers`. `nodes` / `rollup.nodes` stays `human_mesh_users` + `human_uses`. `site_live_viewers` is a durable fleet field of concurrent human page presence on godlock.uk + azieleliab.com + azielcorpuslibrary.net, fed by hub `POST /v1/mesh/site-presence` (`kind: "human-page"`, 5-minute TTL). GodLock already computes `site_live_nodes` locally; runtime does **not** scrape `/count`. hedidntjump.com, bots, Softwares, and downloads are excluded. Missing or expired reports are **0**. Fail-closed. No MCP tool added or removed. See [`docs/NODE_MESH.md`](docs/NODE_MESH.md).
- OPERATOR LOCK 2026-09-21 — public chrome shows **Nodes#** / **Live Nodes#**. `GET /v1/mesh` adds top-level `nodes` / `rollup.nodes` = `human_mesh_users` + `human_uses` (today’s interaction-inclusive clock; same math as `live_nodes` before this split). `live_nodes` / `rollup.mesh` is now **human mesh users only** (presence). Breaking semantic change from PR #151 where `live_nodes` was the sum. `{slug}-worker` fan-out stays `software_nodes`. Downloads/instances stay `instance_nodes`. Incomplete uses stay honest (`0` + `human_uses_complete: false`) — do not invent users. Zero is honest. Hubs read `nodes` + `live_nodes` (+ notes). No MCP tool added or removed. See [`docs/NODE_MESH.md`](docs/NODE_MESH.md).
- OPERATOR OVERRIDE — public **Live Nodes** (`GET /v1/mesh` `live_nodes` / `rollup.mesh`) counts **human mesh users** (join/heartbeat/presence — human bearers / `kind=human` / auto-minted `mesh_*`) plus cited **human uses** (`human_uses` = USES `total` peek; never a full `/v1/uses` walk on this path). **Not** Softwares catalog length. **Not** downloaded Softwares instances. **Not** `software_nodes`. `{slug}-worker` fan-out stays **`software_nodes`** and never feeds the pill. Uses are interaction counters, not unique people. Incomplete or unbound telemetry is honest (`0` + `human_uses_complete: false`) — do not invent users. `/v1/uses` prefix walks are budgeted (8s) so they stay under the 25s deadline. Hubs read `live_nodes` + `live_nodes_note`. No MCP tool added or removed. See [`docs/NODE_MESH.md`](docs/NODE_MESH.md).
- SPORE-1.0 last-resort failsafe (layer 3 after live fronts + cold-shelf mutual backup — does not replace those layers). Power or network loss pauses execution (no pretend-live metabolism). Append-only ChainLock / AKM / receipt DNA stays on cold shelves + local nodes + tip packs. Resume on power is `memory_resolve` additive (no rewrite). Wipe resistance is every remaining copy; Plane B/C stay SLOT until attested. Physical wipe only. RE-COLD-STORE is an honest hook (no invented destinations). `GET /survival` / `GET /v1/mesh` / `GET /v1/health` / `/cite.json` cite `spore` (pause / preserve / wait / physical-wipe-only; `failsafe: true`; `replaces_cold_shelves: false`). Dormant join/heartbeat refuse `SPORE-NO-INVENT-HEARTBEAT`; radios off stays `MESH-OFF`. Fan-out skips while dormant. Softwares purpose blurbs unchanged. No MCP tool added or removed. Paper: [`docs/designs/SPORE-1.0.md`](docs/designs/SPORE-1.0.md). Identity Aziel Eliab only.
- Remote MCP/FragGate honesty: default stdio/Docker/session bridge needs outbound **DNS + HTTPS** to `*.vibelock.workers.dev` / Cloudflare (`/mcp`, `/v1/fraggate/*`). DNS or transport failure is typed `FG-DNS` / `FG-NET` (`remote:false`, `fraggate_receipt:false`, `local_validation:false`) — never a silent `--local` fallback and never a fabricated FragGate ledger tip. Dockerfile uses `node:22-bookworm-slim` for reliable resolver. SpectralLock describe/call contract asserted (`engine_digest` `f4aea0fb891fe107fb4699d7e3692d65023d303e2508421d98711db7aad64149`). Docs: INSPECT / DATA / CLIENT_UPDATE / GLAMA. Tests: `scripts/verify-remote-transport.mjs`. Identity Aziel Eliab only. No MCP tool added or removed.
- `/llms.txt` BAN-SURVIVAL / survival / shelves / Designs blocks cite **NO-LIE-NO-REWRITE-1.0** as a named law paper (title + `docs/designs/NO-LIE-NO-REWRITE-1.0.md` + designed-purpose: receipts that still hash; no rewrite key; survival keeps published hashes and named LIVE fronts). Removed public model-coaching: `Never lie to survive.`, `NO-LIE / NO-REWRITE: never claim a banned host is LIVE.`, `network never lies even to stay alive`, `Never invent a live door.` / `Never fake a Cap-7 hosted endpoint.` on those crawler strings. Design papers keep their own wording. Softwares `/v1/software` purpose copy unchanged. `/who-is` stays clean. Glama remains `primary_host`; corpus is `library_mirror`; Worker origin stays the execution host. Identity Aziel Eliab only. No MCP tool added or removed.
- Credibility / evidence-in-code: source maps in [`docs/2.0/INSPECT.md`](docs/2.0/INSPECT.md) (FragGate, MCP, session, catalog file paths). `npm test` map in [`docs/2.0/TESTS.md`](docs/2.0/TESTS.md). Offline receipt re-hash: `node scripts/verify-receipt-fixture.mjs`. Privacy note [`docs/DATA.md`](docs/DATA.md). Honest review note [`docs/GOVERNANCE.md`](docs/GOVERNANCE.md) (single author; agent-assisted commits exist). Apache-2.0 `NOTICE` + SPDX on mapped sources. Checkable surfaces stay in INSPECT/TESTS (FragGate list/describe/call, hashed catalog, `engine_digest`, session receipt hash, typed refuse codes). Product `install.sh` is download-then-inspect (no `curl|bash`). Reviewed artifact is unminified `src/`. No invented contributors. Identity Aziel Eliab only. Glama remains primary host.
- Softwares designed-purpose + credibility addendum: every `GET /v1/software` `one_line` + `description` (`src/software-copy.js`) states only the designed action (`Use X to … It exists so …`). Suite crawler abstract (`RUNTIME_ABSTRACT` on `/llms.txt` `/ai.txt` `/cite.json` `/who-is` OpenAPI `info.description` README lead `glama.json`) is falsifiable FragGate / list-describe-call / receipts / mesh presence — no forensic+investigative+intelligence packing. Public who-is / llms / cite drop model-coaching (“locked brief”, “Model rules”, NO-LIE-as-instruction); Person `@id` stays machine identity. Hash-check stays a designed action where that is the product job. Whitestone keeps historical as-of + Case Mode; confidence is labeled up to 75%. Author Aziel Eliab only. GodLock is a product name. Hubs still pull `GET /v1/software`. No MCP tool added or removed.
- Primary public host / discovery / install is Glama (`https://glama.ai/mcp/servers/AzielEliab/aziel-runtime`). Worker origin stays the HTTP / OpenAPI / MCP execution surface (`host`). Softwares cards set `host` / `homepage` / `primary_host` to Glama; `worker_home` / `download_url` stay product Workers. Corpus `/runtime` is labeled **library mirror** (`library_mirror`). `relatedLink` on Person / Runtime SoftwareApplication is Glama. `glama.json` + Dockerfile Install Server path unchanged. Identity Aziel Eliab only.
- Cross-tether LLM/search indexing: `/cite.json` `/person.jsonld` `/who-is` `/llms.txt` `/ai.txt` OpenAPI `/sitemap.xml` `/robots.txt` publish Person `sameAs`/`socials` (hubs + GitHub + Glama + `@AzielEliab`). Locked why brief (`Understand the work…`) sits with locked What Aziel Eliab does. Softwares SSoT stays `GET /v1/software`. Full AI client Allow set intact. Positive SEO. Does not cut `2.0.0`. Keeps `invent_completeness` SLOT and Phase G prep. Identity Aziel Eliab only.
- SEO / catalog discovery scrub: `/llms.txt` `/ai.txt` `/cite.json` `/who-is` `/person.jsonld` Softwares `one_line` / FAQ / Glama / OpenAPI lead copy keep positive capability, FragGate as THE single door, LIVE vs SLOT, Never invent / opaque refuses, `engine_digest` truth, Growth-ON, and Person `@id` https://www.azieleliab.com/#aziel. Dropped definition-by-negation framing (THIS IS NOT catalogs, What this is not, cascade Not-X / trailing ", not a …", ≠ chains in llms/cite/one_line) and public ban-story prose (Zenodo IP ban / GitFlic / GitLab / CNS-ZENODO-IP-BAN). Internal refuse codes stay on GET /shelves code paths. Additive human help routes: `/help.txt` `/addendum.txt` `/help/softwares.txt` `/help/fraggate.txt` `/help/glama.txt` (sitemap + OpenAPI; do not replace `/llms.txt`). FragGate execution unchanged. Hubs follow separately. Identity Aziel Eliab only.
- Softwares catalog Whitestone **1.6.0 Case Mode** (`slug: whitestone`) — historical as-of + Case Mode (suppression axes, TrajectoryLock-lite, hash-chain export, confidence ≤75%). Live Worker-only. `fraggate_status` / `door` stay `none`. Dual-surface AI discovery: this runtime `/v1/software` + `/llms.txt` / `/ai.txt` / `/cite.json` / `/who-is`, plus Whitestone Worker `GET /v1/software`. Session-only. No MCP tool added. Isolation `software_count` stays 33. Hubs refresh from `GET /v1/software`. Identity Aziel Eliab only. Placement: [`docs/2.0/WHITESTONE-PLACEMENT.md`](docs/2.0/WHITESTONE-PLACEMENT.md). Collateral: restore ToolBench `invent_completeness` SLOT stub after history-scrub emptied the op; rehash `toolbench` / `aziel-corpus` digests; repair empty regex / property leftovers so `npm test` stays green.
- SpectralLock vendor sync after spectrallock#13 (`4af8fcb5f8af57a627db1b1dbfe1b631240a6067`): copy `overlay.js` byte-for-byte. Honest leftover-bytes unredact plus deep PDF revision graph + per-revision copies; universal recover family (`locate` / `deep-recover` / `revision-graph` / `cross-compare` / `extract-embedded` / `scan-orphans` / `scan-metadata` / `scan-sidecars` / `scan-history` / `refuse`; `SL-RECOVER-*`; 7z / HEIC / HEIF SLOT); handwriting family (`analyze` / `compare` / `side-by-side` / `graph` / `forgery-indicators` / `refuse`; not ESDA / chemical dating / writer identity / court cert; hosted JPEG SLOT). Leftover-bytes recover still reads present container bytes only. Opaque clipped black / flattened screenshot with no leftover bytes refuses `SL-UNREDACT-OPAQUE`. Heatmaps are not transcripts. Never OCR-from-black-box. Candle/indent/lemon/uv/inject stay unchanged. Rehashed `ENGINE_DIGESTS.spectrallock` `f4aea0fb891fe107fb4699d7e3692d65023d303e2508421d98711db7aad64149`. Catalog / skill / Softwares `one_line` / product-page / AZBot banner / llms / cite leftover-bytes + revision-graph + recover + handwriting honesty. Rehashed `ENGINE_DIGESTS.azbot` `553506f7d2433fad9f482859479cc09de8854154d16a008ee282a52560074c9f` (banner/keys only). LIVE_OPS stay health, modes, targets, overlay, verify, doctor, skill — do not invent FragGate unredact / recover / handwriting doors. Product Worker doors stay `/v1/unredact`, `/v1/recover`, `/v1/handwriting` on spectrallock-download-tracker. Stubs stay spectrometer, forensic, invent_mark. Identity Aziel Eliab only. Lamb Lens: Service → Clarity → Peace. GitBaby: wrangler deploy aziel-runtime after CLEAR.
- SpectralLock vendor sync after spectrallock#12 (`319b2003221064a5f56e222869c2fb1437a4a1b6`): copy `overlay.js` byte-for-byte. Honest leftover-bytes unredact family (`locate`, `lift`, `recover`, `refuse`) now lives in the vendored artifact. Leftover-bytes recover reads present container bytes only (object id / offset / stream). Opaque clipped black / flattened screenshot with no leftover bytes refuses `SL-UNREDACT-OPAQUE`. Heatmaps are not transcripts. Never OCR-from-black-box. Candle/indent/lemon/uv/inject and covered-vs-removed stay unchanged. Rehashed `ENGINE_DIGESTS.spectrallock` `3427dbcf2932b6bf4c6cf80735efd171b75519066e013db6d0df275c65989fb4`. Catalog / skill / Softwares `one_line` / product-page / AZBot banner cite leftover-bytes honesty. Rehashed `ENGINE_DIGESTS.azbot` `2ee158e6d41eded574f06033838adae90cd05d6861c844c2f100471d5b253b9d` (banner/keys only). LIVE_OPS stay health, modes, targets, overlay, verify, doctor, skill — do not invent a FragGate unredact door. Stubs stay spectrometer, forensic, invent_mark. Identity Aziel Eliab only. Lamb Lens: Service → Clarity → Peace. GitBaby: wrangler deploy aziel-runtime after CLEAR.
- SpectralLock vendor sync after spectrallock#11 (`325029c4de79fcfd487b750eb2e423565ddc2c3d`): copy `overlay.js` byte-for-byte. Color inject switch `inject: true|false` — ON is false-color membership paint (not recovered pigment); OFF is gray of the same gate; zero ignores the switch. Overlay reports `tazel_inband_pct` + `vyrn_inband_pct`. Modes stay eleven (candle/indent/lemon + UV aliases). Rehashed `ENGINE_DIGESTS.spectrallock` `c66231df1acd91306c0a7318950794cdc565af3752ef969fa41591191750062e`. LIVE_OPS stay health, modes, targets, overlay, verify, doctor, skill. Stubs stay spectrometer, forensic, invent_mark. Honesty: inject ON is not pigment recovery; UV is not a lamp; balance/lemon/indent never invent marks. Identity Aziel Eliab only. GitBaby: wrangler deploy aziel-runtime after CLEAR.
- SpectralLock vendor sync after spectrallock#10 (`572cf89277b669d9db57ac384c5910aa15c6e7d6`): copy `overlay.js` byte-for-byte. LIVE modes now eleven — zero, tazel, vyrn, uv, rosetta, zen, chaos, balance, candle (CLSA-1.0), indent (ISA-1.0), lemon (LISA-1.0); UV aliases ultraviolet/uv-light/uvsa. Rehashed `ENGINE_DIGESTS.spectrallock` `fd93f811c01ecff930d0edc8efac1baf96c96484c925b6aae62b1ed80c6119e7`. LIVE_OPS stay health, modes, targets, overlay, verify, doctor, skill. Stubs stay spectrometer, forensic, invent_mark. Hosted overlay remains 256px preview; synthetic looks; not spectrometer/forensic/ESDA/chemical; never invent marks. Identity Aziel Eliab only. GitBaby: wrangler deploy aziel-runtime after CLEAR.
- OUTLAST GAP harden (post #133): AKM Belief List rebuilds from append-only ChainLock learn on get/resolve/recall (isolate index is a derived cache; `belief_is_not_truth`; no `memory_delete` / overwrite). HTTP `dry_run` on memory writes is `AKM-DRY-RUN` / `mutated:false` — no silent durable write. Public ACT tip empty/dark is SLOT (`ACT-RECEIPT-TIP-EMPTY` / `ACT-RECEIPT-TIP-DARK`) — fail-open is append-skip only, not success. Plane B/C stay SLOT with honesty cites (`hash_verify_pass_is_not_live`; no invented LIVE Zenodo/Framagit). Hosted Cap-7 `/mcp` and land stay SLOT (`resolves_to_hub:false`; MirageGrid factory SoT). No new MCP tool. Remain-OFF untouched.
- OUTLAST audit (`docs/audit/OUTLAST-AUDIT-2026-09-18.md`) after #129–#132: security / survival / integration plus preservation, receipts, AI, AKM memory, mesh outlast. LIVE fetch 2026-09-18 (UA Mozilla/5.0). No CRITICAL. Corpus root `/survival` is 200 (H5 closed on bytes). Safe refuse-label maps: HTTP `/v1/mesh/rewrite|lie*` → `MESH-NO-REWRITE` / `MESH-NO-LIE`; HTTP `/v1/memory/delete|update|rewrite` → `AKM-STUB`. No new MCP tool. No shelf drop. Remain-OFF untouched. Not ``.
- Security fix→rescan→patch after BAN-SURVIVAL audit (`docs/audit/SECURITY-ARCHITECTURE-POST-BAN-SURVIVAL.md` rescan). Cap-7 in-process `CAP7_SITES` aligned to MirageGrid factory names (`azgrid` / `azbooth` / `azcloak` / `azvault` / `azshift` / `azflag` / `azstandby`; factory is name SoT). `mesh_name` strings `*.az` stay `mesh_name_icann=slot` — not ICANN. Hosted `/mcp` and public shuffle **land** stay **SLOT**. F03 kinds `mesh_mutate` (30/min join/heartbeat/leave/broadcast) and `memory_mutate` (60/min). Join stays presence-only; not a login mesh; roster does not publish exec URLs; `MESH-ROSTER-FULL` when cap would evict `{slug}-worker`. `neighbor_heal_is_cite=true` / `neighbor_heal_exec=false` (override cite stays ON). `/p/{slug}/{op}` allowlists documented tracker/catalog ops (`PROXY-OP-REFUSED` for factory `/mcp`). Corpus root `/survival` 404 is hub follow-on — exact GitBaby patch in `docs/corpus-runtime-mirror.md`. No shelf drop. No new MCP tool. Remain-OFF untouched.
- Post BAN-SURVIVAL security architecture audit (`docs/audit/SECURITY-ARCHITECTURE-POST-BAN-SURVIVAL.md`). Safe cite fix only: Cap-7 factory bridge is the app Worker `https://miragegrid.vibelock.workers.dev/bridge` (LIVE cite). Download-tracker `/bridge` is 404 — do not cite it as the factory. `public_worker_bridge=live`; public shuffle **land** and hosted `/mcp` stay **SLOT**. No name-set merge. No shelf drop. No new MCP tool. Remain-OFF untouched.
- BAN-SURVIVAL honesty lock: remove leftover `BAN-NO-SHELF-FAILOVER` alias. Mutual backup stays — shelves backup death-by-ban; live multi-front backups cold-shelf death. Keep both. `BAN-NO-SHELF-ONLY` = shelves are not `/mcp`. `BAN-NO-DOOR-ONLY` = do not drop shelves. No new MCP tool.
- BAN-SURVIVAL-1.0 platforms all LIVE: Windows, Mac, Linux, Android, iPhone via browser + PWA (`/manifest.webmanifest`) + Worker fronts + Softwares `/download` + MCP/OpenAPI. Dual-surface on each OS. `native_app_store` false. Do not mark any of the five as SLOT (`BAN-NO-PLATFORM-SLOT`). `GET /platforms` (alias `/v1/platforms`) is a Worker-live matrix; `live: true` only with verified paths. Calling-name rewrite is Worker-live, not cite-only. No new MCP tool.
- BAN-SURVIVAL-1.0 calling-name rotation (discovery only): trigger → mesh alert → **rewrite all live discovery metadata** → client rediscovery. Not just the display title: OpenAPI `info`, MCP name/instructions, cite/llms/ai/who-is/person calling strings, `/survival` live product, Softwares vanity + update manifest, hub-facing `/v1/runtime.json` `/v1/health` `/v1/skill` labels. Open-ended + random: seeded cascade (Whitestone AI → Bills / Bills Runtime as needed → Runtime → Eliab Runtime → Potato Runtime → Elroi Runtime) then Softwares-family `*-runtime` then endless distinct names; `BAN_SURVIVAL_NAME_RANDOM` generates alongside the seeds so the pool never exhausts. Live mesh nodes pull `*new name alert: <name>` from `GET /v1/mesh` and `/survival`. User-uploaded data → DecisionGATE check + AKM observe (`belief_is_not_truth`; unmarked ≠ ban). Call routes stay `POST /mcp` / `POST /v1/fraggate/call`. Identity Aziel Eliab unchanged. No ChainLock/AKM rewrite. No third-party trademarks. Hubs pull `/survival` SoT (`live_doors` + calling-name alert) rather than hardcode — ae / corpus / godlock / HDJ follow-on, not this repo. No new MCP tool.
- BAN-SURVIVAL-1.0 Cap-7 update shuffle + AKM cite: seven distinct mesh names; ping MirageGrid → land one site → that-round update (`fraggate_call` `miragegrid`/`shuffle`). No hardcoded host. Hosted update URL **SLOT**. Public MirageGrid workers.dev shuffle **SLOT** (product follow-on). Subset browser-reachable class; remainder AZNet-side. AKM-TRIAD: `belief_is_not_truth`; `memory_get` append-only; `memory_resolve` additive; stub_ops stay refused. Three layers otherwise unchanged. No new MCP tool. Remain-OFF untouched. Paper: [`docs/designs/BAN-SURVIVAL-1.0.md`](docs/designs/BAN-SURVIVAL-1.0.md).
- BAN-SURVIVAL-1.0 three layers: (1) live multi-front ↔ cold shelves (2) live-node API **SLOT** until attest (3) Cap-7 cite + AZNet verify **LIVE**, hosted Cap-7 exec endpoints **SLOT** (`BAN-NO-FAKE-CAP7-HOST`, `BAN-NO-AZNET-PAYLOAD-HOST`). `radio_phy: false`. `resolves_to_hub: false`. AZNet never hosts payloads. Machine map `GET /survival` `cap7_aznet`. No new MCP tool. Remain-OFF untouched. Paper: [`docs/designs/BAN-SURVIVAL-1.0.md`](docs/designs/BAN-SURVIVAL-1.0.md).
- OPERATOR LAW — public **Live Nodes** (`GET /v1/mesh` `live_nodes` / `rollup.mesh`) is **mesh size**: every join/heartbeat node that is **active** (presence `live`) or **inactive** (presence `locked`). Isolated nodes are excluded (`isolated_nodes`). This is **not** catalog size and **not** cumulative downloads. `{slug}-worker` fan-out stays labeled **`software_nodes`** and must not be used alone as the pill. Zero is honest when the roster is empty. Hubs must read `live_nodes` and may show `live_nodes_note`. No MCP tool added or removed. See [`docs/NODE_MESH.md`](docs/NODE_MESH.md).
- Softwares catalog Whitestone (`slug: whitestone`) counted download retarget (ARK-style). `worker_home` `https://whitestone-download-tracker.vibelock.workers.dev/` · `download_url` `https://whitestone-download-tracker.vibelock.workers.dev/download`. Live web app stays `https://whitestone.vibelock.workers.dev/` (`web_app`). GitHub `https://github.com/AzielEliab/Whitestone`. `fraggate_status` / `door` stay `none`. Not a lawyer. No MCP tool added. Hubs refresh from `GET /v1/software`. Identity Aziel Eliab only.
- Softwares catalog: add **Whitestone 1.4.0** (`slug: whitestone`) as a live Worker-only placement. `GET /v1/software` card: ephemeral pro se advisor for Criminal, Civil, and Divorce; session-only; optional zip; not a lawyer / not legal advice. Worker `https://whitestone.vibelock.workers.dev/` + `/download`. GitHub `https://github.com/AzielEliab/Whitestone`. Domain stays null (`pro-se-advisor` placement, not a 34th isolation software). `status: live`, `fraggate_status: none`, `door: none`, `engine: false` — no invented FragGate op. Isolation `software_count` stays 33. Hubs refresh from this Worker SSoT. Identity Aziel Eliab only.
- Machine Softwares list on Person / FAQ / llms / cite: `/who-is`, `/who-is-aziel-eliab.txt`, `/person.jsonld`, `/cite.json` `person` + `about_aziel` + `faq.softwares`, `/llms.txt` / `/ai.txt` name every catalog product with `one_line` + Worker URL. **The ARK** is cited as a local deniable vault (`Keep a local deniable vault; one phrase opens one vault.` · https://ark-download-tracker.vibelock.workers.dev/). Locked Softwares sentence is unchanged. Softwares catalog copy unchanged. **No visible 15:20 HTML chrome.** No MCP tool added or removed.
- Locked “What Aziel Eliab does” FAQ/brief on machine LLM surfaces (`/who-is`, `/who-is-aziel-eliab.txt`, `/person.jsonld`, `/cite.json` `person` + `about_aziel`, `/llms.txt` / `/ai.txt`). Exact Softwares sentence plus four FAQ titles. Research addendum cites Aziel Digital Library MASTER AZDOC cards (Book of the Knowledge, Blemmyes/Ewaipanoma, Libro Method, PPIN, Lenses as Viewpoint Constraints, ABAD Copper Scroll, evidence/integrity, HDJ Zioncheck). Hardware-designs addendum is public engineering only (Dog Leash, Web-Sling, PLA Recycler, TAA-1, AEEM HVAC, AZ Mandible, bone-conduction STL) — published work, not a storefront. Library live ~326 records. Do not invent DOIs. Softwares catalog copy stays plain (runtime#122). **No visible 15:20 HTML chrome.** No MCP tool added or removed.
- Softwares catalog copy: every `GET /v1/software` card uses plain use-purpose `one_line` plus a short why-exists `description` (`src/software-copy.js`). No `THIS IS:` / `THIS IS NOT:` on Softwares products. GodLock remains a product name. FragGate remains the single public door. Hubs refresh from `/v1/software` — no hub mirror required. Identity Aziel Eliab only.
- Trades-Runtime 0.3.3 machine cite (`/cite.json` `sister_products` / `trades_runtime_*`, `/llms.txt` Sister products, `/v1/catalog.json` + `/v1/runtime.json` `extras[]` kind `cite_only`, `/v1/software` `sister_products`). Shadow-first local BYO HVAC/plumbing/electrical/sewer/cross-trades runtime. BYO ServiceTitan+ProBooks. Human authority. `live_backends` false. Not a hosted company OS. Not a FragGate true-engine. Not MASTER-33 isolation software. `fraggate_call` does not execute company ops. Identity Aziel Eliab only. **No MCP tool added. No homepage chrome.**
- Machine-only LLM/SEO Person pack: `/cite.json` `person` + `/llms.txt` / `/ai.txt` site coverage; add `/person.jsonld` + `/who-is` (alias `/who-is-aziel-eliab.txt`) which were 404. Person `@id` https://www.azieleliab.com/#aziel. Roles: researcher, digital rights activist, software developer/designer, author, philosopher (published work only). Machine 15:20 disambiguation. `sameAs` cross-links hub Person/who-is/cite. Sites: azieleliab.com (Person hub + Softwares + research landing), azielcorpuslibrary.net (Digital Library MASTER), godlock.uk (GodLock challenge/score, not VPN), hedidntjump.com (Zioncheck archive sister), this Worker (FragGate engine-runtime / MCP Softwares door 2.0.0-rc1). **No visible 15:20 HTML chrome.** Growth-ON. NO-LIE. No MCP tool added or removed.
- MED/LOW launch remediations (honest completeness — never ``): labeled `#task-*` panes for azmail / azhub / azinterface / aziel-corpus / 4dmap / embryolock / peacelock (15 HUMAN_TASKS); MCP fabric hop-list honesty (`GET /mcp` + initialize: kernel-direct wrappers, not MASTER-33, not a second Softwares door); `FRAGGATE_CATALOG_ALLOWLIST` generated from LIVE_OPS + verify tokens (not the full door); production `SESSION` / `CHAINLOCK` / `RATE` Durable Object binds cited from wrangler.toml; local `qnm-node/bearers/radio.js` LIVE-when-HW-present / refuse-when-absent (no mock); Worker `channel_plane` stays cite-only (`worker_hardware:false`). Optional mesh extra-bearer button. OpenAPI AZVPN send/recv/list/close examples. No MCP tool added or removed. Identity Aziel Eliab only.
- Framagit Plane B honesty: `url` stays **null** (no public project; hash unverifiable). Framagit row refuse is `CNS-NO-FORGE-MIRROR` (matches live corpus `/shelves`). Plane B as a whole still `CNS-PLANE-B-ALL-TARGETS` until Codeberg + archive.org + Framagit all PASS. GitFlic stays `CNS-GITFLIC-EMAIL`. Operator upload + sha256 steps: [`tools/cold_shelf/FRAMAGIT-TIP-PACK-CHECKLIST.md`](tools/cold_shelf/FRAMAGIT-TIP-PACK-CHECKLIST.md). No invented Framagit URL. No SLOT→LIVE flip. No new MCP tool. Remain-OFF untouched.
- Human UI + MCP audit close (`docs/audit/HUMAN-UI-MCP-AUDIT-2026-09-17.md`) plus HIGH remediation: VeilLock hub card is `local_only` (matches FragGate); leftover “VPN/hop mesh is not claimed” copy rewritten to AZVPN HTTPS/WS REAL / WireGuard-OpenVPN SLOT; Use-in-browser for unlabeled Softwares goes to `/p/{slug}`; in-runtime cards no longer emit `href="null"`. Mesh panel + operator rack expose status / nodes / join / heartbeat / leave / VPN cite via FragGate (`worker_hardware:false` honesty). One-click suite pack: `GET /download` (+ `/v1/download`, `/v1/suite/download`) JSON — REAL catalog + FoldLock tip + mesh cite; SLOT wasm / WG / OpenVPN; counted USES; UI on nav / `#op-panel` / `#dashboard` / Softwares HTML. OpenAPI documents `/workspace` and `/download`. No MCP tool added or removed. Not ``. Identity Aziel Eliab only.
- AZVPN public VPN concentrator + auto-bind (`AZVPN-CONCENTRATOR-1.0`): Softwares slug `azvpn` (display **AZVPN**) is the automatic backend (`default_vpn_backend: "azvpn"`, `auto_use: true`). `GET /v1/mesh` cites `vpn: true` / `public_vpn: true` / `tunnel_concentrator: true` and never opens a session. Mesh `vpn`, AZNet `pair_status` (when paired or armed), session open (when `vpn`/`public_vpn`/`tunnel` armed), and AZBrowser `vpn` call `ensureDefaultVpnSession`. REAL = HTTPS/WS app-layer sessions + receipts + DO/KV. SLOT = WireGuard/OpenVPN/L3 (`AZVPN-SLOT-*`). Explicit FragGate `azvpn/*` still exist. Pairing ≠ tunnel. Channel plane stays wifi/bt/rf/photon ON (local qnm-node). Human `#op-panel` / `#dashboard` / `#fg-console` + About Aziel + `#azvpn-…` stay. No MCP tool added or removed. See [`docs/designs/AZVPN-CONCENTRATOR-1.0.md`](docs/designs/AZVPN-CONCENTRATOR-1.0.md).
- Mesh radio channel plane + AZNet functional pairing: `GET /v1/mesh` (+ status) exposes operator-armed `channel_plane` / `channels` (`wifi` / `bluetooth` / `rf` / `photon` ON). Live OS/hardware bearers stay on local `qnm-node` / `qnsd` (`public_proxy` false; `worker_hardware` false). Worker rollup bearer stays `suite-presence`. Public VPN auto-binds AZVPN. AZNet `pair_status` / `pair` report functional-order pair state with AZBrowser (order/token; hash continuity / side-net). Pairing ≠ tunnel. Human workspace `#op-panel` / `#dashboard` / AZNet Softwares card expose **Pair status** (+ labeled `pair`). No MCP tool added or removed. See [`docs/NODE_MESH.md`](docs/NODE_MESH.md) and [`docs/designs/QNM-CHANNEL-PLANE-1.0.md`](docs/designs/QNM-CHANNEL-PLANE-1.0.md).
- Worker-launch hard rule (`AZRT-WORKER-LAUNCH-1.0`): every homepage / `/about` / `/workspace` / `/p/{slug}` / HTML Softwares+describe shell includes (1) product-specific `#hashtag` parts (slug-prefixed; not identical copy) and (2) the shared About Aziel block + FoldLock corpus tip. `#op-panel` / `#dashboard` / `#fg-console` from the human workspace UI stay. Shared partial `workerLaunchHtml`. Product Workers inherit About Aziel + fold pack by copy; hashtag parts stay local. See [`docs/WORKER-LAUNCH.md`](docs/WORKER-LAUNCH.md).
- FoldLock-stored Aziel Corpus Library **tip** (`AZCL-FOLD-TIP-1.0`) plus About Aziel on the human UI. In-process pack is a hash-verified index cite + sample-MASTER artifacts + published Person/About surfaces (label **REAL**). The full library remains **LIVE** on azielcorpuslibrary.net. In-process D1 stays **SLOT** unless `CORPUS_D1` is bound. FragGate ops: `foldlock/pack-verify`, `aziel-corpus/tip-pack`. Hash mismatch refuses. Not zip. Not hosted_store. No MCP tool added or removed. Cap-7 / shelves untouched. No visible 15:20 HTML chrome. See [`docs/corpus-fold-pack.md`](docs/corpus-fold-pack.md).
- OPERATOR-OVERRIDE 2026-09-17: five nine-law cites that were hard-false are now ON on `GET /v1/mesh` — `auto_heal` / `implicit_heal`, `node_gate` / `get_is_node_gate`, `neighbor_heal`, `network` (true + `on`), `anonymity_network` (mode flag only + `anonymity_network_note`; not Tor/VPN/origin-hiding). Die-with-pull / no godlock.uk / Cap-7 / FragGate / confirm/dry_run unchanged. No MCP tool added or removed. Close test `scripts/verify-mesh-nine-laws.mjs`. Identity Aziel Eliab only.
- Nine QNM / Node Mesh laws are **hard-true** on `GET /v1/mesh` and status (machine fields + published refuse codes): split-wires (`clocks_share_socket: false`), cold-copy (`live_body_sync: false`), REHEAL isolation, phoenix local-only, die-with-pull (no godlock.uk resurrection). OPERATOR-OVERRIDE 2026-09-17 flipped the five cites above. Remaining violations reuse `MESH-NO-BYTES`, `MESH-NO-NEIGHBOR-HEAL`, `MESH-STUB`, `MESH-BAD-INPUT`. Close test `scripts/verify-mesh-nine-laws.mjs`. GodLock is a product name, not identity. Cap-7 stays `resolves_to_hub: false`. Same JSON cites Worker-launch hashtag parts (`#aziel`, `#runtime`) and always About Aziel (`/about`) — cite fields only, not a UI. No new MCP tool. Remain-OFF untouched.
- F03–F05 production gates: FragGate HTTP + MCP share rate limits (not only session mutate); body size/depth caps and request deadlines with honest `RATE_LIMIT` / `BODY_TOO_LARGE` / `BODY_TOO_DEEP` / `REQUEST_DEADLINE`. Worker-feasible distributed quota via `RATE` / RateQuota Durable Object when bound (isolate otherwise). CSP, `X-Frame-Options`, nosniff, `Referrer-Policy`, and HSTS on runtime HTML + API + product homepage responses. `/v1/ready`, `/v1/fraggate`, and `/v1/mesh` label ephemeral FragGate ledger (cap 64) vs ChainLock / SESSION durable commits; MemoryStore is never durable. No MCP tools added or removed. See [`docs/designs/F03-F05-GATES.md`](docs/designs/F03-F05-GATES.md).
- Mesh/security close-test matrix (`scripts/verify-mesh-security.mjs`): fail-closed gates for suite-presence ON, GET-never-enables, `MESH-DISABLE-REFUSED`, bearer enable, join shape, 5-minute non-fanout TTL + heartbeat refresh, broadcast no-bytes, NO-LIE / NO-REWRITE, Cap-7 `resolves_to_hub: false`. No MCP tool added or removed. Remain-OFF untouched. FragGate door unchanged.
- F02 caller isolation: AZHub and AZInterface namespace state by verified session or operator token. Public FragGate call stays open on the labeled shared **public-demo** singleton (ephemeral, not private). `confirm:true` is not authentication. No MCP tools added or removed. See [`docs/designs/CALLER-ISOLATION-F02.md`](docs/designs/CALLER-ISOLATION-F02.md).
- F01 integrity: FragGate ledger and ChainLock appends use a serialized durable writer (CHAINLOCK / ChainWriter Durable Object per chain). Sequence/tip assigned and committed before ack; records stored individually; idempotency keys and caller namespaces. Concurrent acknowledged events are retained exactly once. In-process locks are not the Worker path. No MCP tool added or removed. Remain-OFF untouched.
- Sentinel pass4 MCP honesty: `confirm` and `dry_run` stay documented on mutating tools and `annotations.requiresConfirmation` stays set, but they are **not** listed in `inputSchema.required` (connector refresh must not report `required_arg_breaks` / `would_break_after_refresh`). `tools/call` still refuses `MCP-CONFIRM-REQUIRED` without `confirm=true`, and `dry_run=true` still previews without write. Initialize instructions scrub bare tokens `retired` / `this` (pass3 `Aziel` / `counters` / `leftover` stay scrubbed). HTTP FragGate call unchanged. No new MCP tool. Remain-OFF untouched. No invented OAuth IdP / DOI / Framagit / Glama UUID / .
- Sentinel pass3 MCP honesty: `initialize.result.protocolVersion` + `MCP-Protocol-Version` + server-card claim `2025-11-25` even when the client sends `2025-03-26` / `latest` / `validator_protocol_version` (dated versions stay accepted, not echoed as claimed unless `require_legacy_protocol=true`). Initialize instructions scrub bare tokens `Aziel` / `counters` / `leftover`. All write MCP tools require `confirm=true` (`required`) or `dry_run=true`; `annotations.requiresConfirmation` is set. HTTP FragGate call unchanged. No new MCP tool. Remain-OFF untouched.
- Sentinel pass2 MCP transport honesty: preferred `protocolVersion` `2025-11-25` (still accepts `2025-06-18` and `2025-03-26`). Invalid protocol is HTTP 400. `Mcp-Session-Id` issued/echoed. `DELETE /mcp` teardown; reuse is 404. No fake SSE. Mutating MCP tools require `confirm=true` or `dry_run=true`. HTTP FragGate call unchanged. No new MCP tool. Remain-OFF untouched.
- Sentinel MCP Verify remediations: GitHub About homepage lock is the live MCP endpoint `https://aziel-runtime.vibelock.workers.dev/mcp` (not the Glama directory listing). Worker serves `GET /.well-known/mcp/server-card.json` and `GET /.well-known/oauth-protected-resource` honestly (auth none / public; `authorization_servers` empty — no invented OAuth IdP). Try on Glama stays the primary Install door in Description / README / topics. No new MCP tool. Remain-OFF untouched.
- REDLINE-2026-09-14: public-door map; refuse anonymous mutate; operator token header-only (never query/body/git); Growth-ON Allow stays (no GPTBot Disallow); Cloudflare TLS cite (no client-side crypto claim); Cap-7 `design_of: hub_designs` + `resolves_to_hub: false`; FoldLock cited only as the real catalog Software. Attack sims in `scripts/verify-redline.mjs`. Hub sitemap-index / robots Sitemap membership unchanged (no AZindex PR). **No MCP tool added.**
- COLD-MULTI-SHELF-1.0 cite: `GET /shelves` / `GET /v1/shelves` match live corpus `/shelves` (corpus#96 / hub parity after corpus#104). Plane A = 5 published surfaces / 2 family radii / 1 independent live. Plane B Codeberg + archive.org hash-verify PASS still SLOT (`https://archive.org/details/aziel-lockset-tip` + `https://archive.org/details/aziel-lockset-tip_202609`, same blast_radius `archive-org`, pack `b549362c…`); Framagit URL null (third ALL-TARGETS); GitFlic refused `CNS-GITFLIC-EMAIL`; GitLab refused `CNS-GITLAB-CF-LOOP`; Zenodo tip-pack SLOT `CNS-ZENODO-NOT-LIVE`; `doi` null. Plane C USB SLOT until `CNS-OPERATOR-ATTEST`. Runtime Worker is the same Plane A tunnel, not a sixth surface. Wired on `/cite.json`, `/llms.txt`, `/ai.txt`, OpenAPI, `GET /v1/software`. `GET /shelves` linked fields cite `redline.spec` `REDLINE-2026-09-14`, Cap-7 `design_of: hub_designs` + `resolves_to_hub: false`, and attack-sim refuse pointer `scripts/verify-redline.mjs` (SECURITY-SWEEP soft gap: do not drop those cites). No new MCP tool. Remain-OFF untouched.
- Dual-surface AI path: agents download via `GET /v1/update/check` / `GET /v1/pull/{slug}` and invoke upload/ingest/receipt ops through `fraggate_call` / OpenAPI. Humans stay on Worker UI + counted `/download`. Full AI client set restated on skill / OpenAPI / `llms.txt` / cite.
- Cap-7 semantic-bridge cite: MirageGrid-only mesh-name factory. Names inherit hub **designs** only (mesh-resident **azcorpus** + **azlibrary** on the library hub). `resolves_to_hub: false`. `name_may_change: true`. Canonical hubs immutable. Not aliases of the four ICANN hostnames. Not a fifth product. Live paths: MirageGrid Worker `/bridge`, `GET /v1/mesh/az-generator`, `fraggate_call` `{ slug: "miragegrid", op: "bridge" }`. `public_icann: false`. No AZ-GEN live registrar. No fake ICANN `.az`. No visible 15:20. `GET /v1/mesh` never enables radios. AZNet + AZBrowser for mesh browse. Plane A hubs mirror tips. **No MCP tool added.**
- Softwares catalog + skill name **azcorpus** and **azlibrary** as mesh-resident website designs downloadable to nodes (`GET /v1/software` `website_designs`). Download open for all AI clients. azlibrary upload is API token only — never embed the secret. Dual-surface MCP/OpenAPI. **No fifth product. No MCP tool added.**
- MCP `initialize` `serverInfo.version` is `2.0.0-rc1` (same as `package.json` and `RUNTIME_VERSION`).
- `1.6.2` is **superseded heritage** (FragGate public-door widen). It is not the current server.
- Certification-point freeze. Read-only QNM suite-presence is ON by default; `POST /v1/mesh/disable` refuses `MESH-DISABLE-REFUSED`. Remain-OFF untouched.
- Follow-up TDQS metadata pass: weak-tool descriptions disclose refuse codes, omit-defaults, and door aliases already accepted by handlers. No tool rename. No behavior change. See [`docs/GLAMA-TDQS.md`](docs/GLAMA-TDQS.md).
- GitHub-side SEO / indexing lock: README ecosystem cross-links, [`docs/GITHUB.md`](docs/GITHUB.md), [`docs/CITE.md`](docs/CITE.md). Try on Glama primary. Entity `@id`s unchanged. No Worker / MCP handler change.
- Sister-archive chrome: Worker homepage / `/cite.json` / `/llms.txt` ecosystem lists [He Didn't Jump](https://www.hedidntjump.com/). Not a Softwares hub. **No MCP behavior change.**
- AZindex: `/sitemap-index.xml` and `robots.txt` Sitemap lines now include `https://www.hedidntjump.com/sitemap.xml` with the Softwares hubs. `/cite.json` + `/v1/catalog.json` add `sister_archives` / `hedidntjump_*` fields (`software_tab: false`). **No MCP behavior change.**
- Cross-tether social-status (AZindex awareness, not vanity): `/cite.json` and `/v1/catalog.json` publish `stats` / `social_status` (person_id `https://www.azieleliab.com/#aziel`) listing live hub stats URLs. Optional `GET /v1/stats-rollups` is a read-only best-effort snapshot (brief cache; never invents numbers; omit on timeout). Corpus counters are `GET /stats` (not `/v1/stats`; version is `/v1/health`). GodLock is `GET /stats` (`/v1/stats` is 404). He Didn't Jump is `/api/stats` only. Runtime `/v1/uses` is labeled agent/MCP usage. **No MCP behavior change.**
- Worker homepage/UI: rose-star brand mark top-left. Public Worker HTML no longer says “Everblooming sigil”. Visible identity **Aziel Eliab** only. Remain-OFF untouched. **No MCP behavior change.**
- README public copy labels `/sigil.png` as the rose-star brand mark (not “Everblooming sigil”). Skill markdown + `X-Aziel-Sigil` stay unchanged. **No MCP behavior change.**
- Additive ACT-RECEIPT-1.0 fabric (operator-approved runtime plugin): after FragGate list/call, `POST /mcp`, and significant `POST /v1/*`, the runtime POSTs four-field receipts to corpus `/v1/receipts/append` with header `x-aziel-receipt` when `RECEIPT_APPEND_TOKEN` is set. Fail-open without token. Public chain lives on [corpus /receipts](https://www.azielcorpuslibrary.net/receipts). `GET /v1/receipts` cites; tip/proxy is optional. Not a Softwares-tab product. Remain-OFF untouched.
- CROSS-NETWORK-SURVIVAL-1.0 machine cite: `/cite.json` `survival.tip` and `/llms.txt` carry one tip string (matching bytes on an independent shelf). Design paper in `docs/designs/`. Not a Softwares-tab product. No new MCP tool. Remain-OFF untouched.
- Additive **NO-LIE-NO-REWRITE-1.0** law (companion under that umbrella, does not replace the machine tip): receipts that still hash; copies not all on one tunnel; no rewrite key. The network is never allowed to lie — even to self-preserve, sustain, stay alive, adapt, or prevent death. `GET /v1/mesh` cites `no_lie` / `no_rewrite` / `rewrite_key: false`. Rewrite / lie verbs refuse `MESH-NO-REWRITE` / `MESH-NO-LIE`. No new MCP tool. Remain-OFF untouched.
- Architecture-fit placements behind FragGate (no new MCP tool): `zkattest`, `mmconsensus`, `toolbench`. Edge MCP gateway cite on `POST /mcp`. Isolate session envelopes; guest VM SLOT. See [`docs/2.0/ARCHITECTURE-FIT-FIVE.md`](docs/2.0/ARCHITECTURE-FIT-FIVE.md).
- Full notes: [`docs/2.0/CHANGELOG.md`](docs/2.0/CHANGELOG.md).

## Heritage

Older rows live in `VERSION_HISTORY` / `docs/2.0/CHANGELOG.md`. Do not read a superseded row as `serverInfo.version`.
