{
  "name": "aziel-runtime",
  "version": "1.4.0",
  "private": true,
  "description": "Aziel Eliab Runtime 1.4.0 — catalog + pull + proxy + session + in-process engines for every catalog Software slug. Author: Aziel Eliab.",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "aziel-runtime": "./cli/aziel-runtime.mjs"
  },
  "scripts": {
    "test": "node scripts/hash-engines.mjs && node scripts/verify-cite.mjs && node scripts/verify-runtime.mjs && node scripts/verify-session.mjs && node scripts/verify-engines.mjs",
    "demo:session": "bash scripts/demo-session.sh"
  },
  "dependencies": {
    "wrangler": "^4.127.1"
  }
}
