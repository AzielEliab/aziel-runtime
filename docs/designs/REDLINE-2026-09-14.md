# REDLINE-2026-09-14 — runtime attack-surface protocols

Author: **Aziel Eliab** only.

Status: LIVE law (2026-09-14). Not a Softwares-tab product. Not a FragGate slug. Not a Remain-OFF flip. Not a 34th Remain-OFF row.

Companion inventories: [SEC-FEAT-1.0](SEC-FEAT-1.0.md), [REMAIN-OFF-BY-DESIGN-2026-09-10](REMAIN-OFF-BY-DESIGN-2026-09-10.md). Executable: `src/redline.js`. Close tests: `scripts/verify-redline.mjs`.

---

Aziel Eliab · law · independently verifiable · Growth-ON Allow stays

    REDLINE-2026-09-14
    Tighten MCP / OpenAPI / FragGate / dual-surface doors.
    Refuse anonymous mutate. Operator token is header-only.
    Never invent Softwares. Never invent Zenodo DOIs.

    Identity: Aziel Eliab only
    Person @id: https://www.azieleliab.com/#aziel (stable)
    Close tests: scripts/verify-redline.mjs

0. Sentence
Map every public door. Rate-limit and refuse anonymous mutate.
Operator token lives in `Authorization: Bearer` or `X-Aziel-Runtime-Token`
only — never query, never body, never git. TLS is Cloudflare edge HTTPS.
Do not claim a second client-side crypto product.

1. Growth-ON (budget)
`robots.txt` stays open crawl. `Allow: /` for `*` and the listed AI/search
agents including **GPTBot**. No `Disallow` for GPTBot “for budget.”
Content-Signal stays `search=yes, ai-input=yes, ai-train=yes`.
Hub sitemap-index lines are unchanged by this paper.

2. Public doors (dual-surface)
Read / cite (public): catalog, skill, health, ready, OpenAPI, MCP
`tools/list`, FragGate list/describe, `/p/{slug}/{op}` (proxy, not exec),
`GET /v1/mesh` (never enables extra radios), `GET /v1/mesh/az-generator`
(Cap-7 cite only), Lamb Lens (fabric after FragGate — not a door).

Mutate (gated): session open/policy/exec/close; mesh enable/join/leave/
broadcast; AKM `rebuild-index`; receipt append. Anonymous mutate refuses.
Public `fraggate_call` stays open for allowlisted live ops. Unknown names
are `FG-HALLUC-TOOL`.

3. Attack sims (must REFUSE)
- Call **AZ Generator** as a registrar / Softwares slug / POST register.
- Enable mesh radios via `GET /v1/mesh` (query or body).
- Inject Cap-7 `resolves_to_hub: true` (or a resolving `design_of`).
- Present a fake Zenodo DOI (`10.5281/zenodo.…` not in the known map).
- Leak the operator token in query, JSON body, MCP args, or responses.

4. Encrypt
Cloudflare terminates TLS for `*.workers.dev` and custom hostnames.
This Worker does not add a browser WebCrypto “vault,” a client-only
cipher, or a FoldLock-as-encryption product. Document the hop. Do not
invent a second lock.

5. FoldLock
Cite FoldLock only as the real catalog Software (`slug: foldlock`,
in-process preview, shared method-paper DOI when wired). Do not invent
a FoldLock Softwares card, a zip engine, or a fifth product. `zip` /
`hosted_store` stay stub.

6. Lamb Lens + Cap-7 + Person
Lamb Lens is fabric ethics after FragGate (`software_tab: false`,
`door: false`). Cap-7 names are `design_of: hub_designs` and
`resolves_to_hub: false`. Person `@id` stays
`https://www.azieleliab.com/#aziel`. Runtime is not the identity hub.

7. AZindex
If hub hosts, `robots.txt` Sitemap lines, or `sitemap-index.xml`
membership change, open an AZindex PR. This paper does **not** change
those hub crawl edges. Additive `/cite.json` fields
(`redline`, `tls`, Cap-7 `design_of`) are fetched on the next cite pull.

8. What this is not
A guarantee. A new Softwares product. A GPTBot block. A client-side
encryption story. A live AZ-GEN registrar. A Remain-OFF enable.

Public identity: Aziel Eliab only. 2026-09-14.
